global:
  scrape_interval: 5s # Aggressive scraping for local sandbox testing

scrape_configs:
  - job_name: "prompt-drift-gateway"
    static_configs:
      - targets: ["gateway:8080"] # Points to our Docker Compose service name
